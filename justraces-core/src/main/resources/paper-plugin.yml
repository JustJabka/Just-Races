name: JustRaces
version: '${version}'
authors: [ JustJabka ]

main: justjabka.justraces.core.JustRaces
bootstrapper: justjabka.justraces.core.JustRacesBootstrap
api-version: '26.2'
load: POSTWORLD

dependencies:
  server:
    packetevents:
      load: BEFORE
      required: true